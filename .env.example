# 开发环境：本地构建
# docker compose up -d
#
# 生产环境：使用 GitHub Container Registry 镜像
# export SERVER_IMAGE=ghcr.io/tania-x/devops-dashboard/server:latest
# docker login ghcr.io -u <username> -p <token>
# docker compose up -d
#
# Agent 测试：
# docker compose --profile agent up -d
